# Maintenance Log

Internal, non-roadmap changes (refactors, bug fixes, doc cleanups, tooling
tweaks, etc.). For user-facing release notes see [../CHANGELOG.md](../CHANGELOG.md).
For roadmap-feature design docs see [plans/](plans/).

**Convention:** newest entry on top. Keep rows terse. Only the 5 columns
below are required — add free-form notes under the table if needed.

| Date | Description | Action | Before | After |
|---|---|---|---|---|
| 2026-06-27 | Sync `zetryn.__version__` to 0.16.0 (matches pyproject.toml). String literal was stuck at 0.13.0 — version-bump scripts only touched pyproject. | bugfix | `zetryn.__version__` reported 0.13.0, three releases behind pyproject. | Both sources agree, package introspection is accurate again. |
| 2026-06-27 | Remove empty `zetryn/providers/` package. Zero-byte `__init__.py`, never imported anywhere. Audit confirmed dead namespace. If M12 ever needs a `providers` namespace it can be re-introduced with actual content. | cleanup | Empty namespace dragged along since early scaffolding, surfaced by v0.16.0 audit. | One fewer dead directory in the public package. |
| 2026-06-27 | Generalize S6 from "Post-Graduation Dip Buy" to "Early-Stage Dip Buy" with `event_type ∈ {launch, graduation}` config. Post-launch settle entry (wait 1–10 min for sniper/bundler dump) and post-graduation dip share the same signal mechanic (wait → retention check → enter), so one agent covers both. | refactor | S6 was scoped only to post-graduation; post-launch settle was a separate uncategorized strategy. Two rows that would have duplicated the same logic. | One S6 row in STRATEGIES + CAPABILITIES covers both events via config switch. |
| 2026-06-27 | Compress `docs/STRATEGIES.md` to strict evidence-based catalog: only strategies validated by mainstream 2026 Solana pro-trader sources (DEXTools, CoinLedger, on-chain analytics). 6 S-tier (4 shipped + 2 planned), 1 A-tier, 5 C-tier rejections. B-tier dropped. Added S6 (Post-Graduation Dip Buy) + A1 (Organic Growth Detector) to CAPABILITIES roadmap. | refactor | Speculative catalog of ~35 strategies generated from imagination (narrative rotation, Telegram alpha, LP injection, failed pump retest, etc. — none cited by pro-trader sources). Redundant, sub-variants of smart money tracking. | Compact 12-row catalog tied to web-validated 2026 mainstream practice. New three-question gate explicitly demands a credible source citation before adding new strategies. |
| 2026-06-27 | Rewrite `docs/STRATEGIES.md` as Solana-memecoin **strategy universe catalog** (~35 strategies ranked S/A/B/C by edge × SNR × data × distinctness rubric). Position management removed from catalog — it's execution layer, kept in CAPABILITIES.md as PL1 only. | rewrite | Prior version was a project-inventory (~10 rows mixing shipped + planned + rejected, plus PL1 mis-categorized as strategy). Did not answer "what strategies exist in the Solana world". | Catalog of the full strategy landscape with `Implemented` column showing what's built. S-tier highlights both shipped (4) and unbuilt build-candidates (5). |
| 2026-06-27 | Restructure `docs/STRATEGIES.md` to table-driven format with explicit **Category** column (Entry agent vs Position management agent). PL1 reclassified from "strategy" to position-management agent — different input shape (`PositionContext`), different action space (sell-side ladder). | refactor | Prose-heavy sections per agent; PL1 lumped under "strategies" alongside entry agents (mis-categorized). | One table per status (Shipped / S-tier / A-tier / Rejected), each row tagged with category. Detailed specs kept for S-tier only. |
| 2026-06-27 | Add strategy catalog `docs/STRATEGIES.md` (shipped + S/A/B/C-tier planned + considered-and-rejected) so future strategy questions have a single answer. Add PL1 (Position Lifecycle Helpers) + S5 (Smart Money Confluence) rows to CAPABILITIES.md roadmap. | add | Strategy decisions scattered across conversation history; no document answering "what's next". | `docs/STRATEGIES.md` is single source of truth; CAPABILITIES.md roadmap lists PL1 and S5 as planned. |
| 2026-06-27 | Documentation layout cleanup — collapse `docs/superpowers/` into `docs/plans/`, introduce this maintenance log for non-roadmap changes, add `docs/plans/README.md` index. | refactor | `docs/superpowers/specs/` held in-flight design docs separately from `docs/plans/` (split, confusing) | Single home for design docs (`docs/plans/`), single home for non-roadmap log entries (this file). `docs/superpowers/` removed. |
